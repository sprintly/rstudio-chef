# shiny-server.conf

description "Shiny application server"

start on runlevel [2345]
stop on runlevel [016]

limit nofile 1000000 1000000

exec shiny-server --pidfile=/var/run/shiny-server.pid >> <%= node['rstudio']['shiny']['log_dir'] %>/server.log 2>&1

respawn
